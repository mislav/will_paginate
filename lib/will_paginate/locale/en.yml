en:
  will_paginate:
    previous_label: "&#8592; Previous"
    next_label: "Next &#8594;"
    page_gap: "&hellip;"

    page_entries_info:
      single_page:
        zero:  "No %{model} found"
        one:   "%{label} 1 %{model}"
        other: "%{label} all %{count} %{model}"
      single_page_html:
        zero:  "No %{model} found"
        one:   "%{label} <b>1</b> %{model}"
        other: "%{label} <b>all&nbsp;%{count}</b> %{model}"

      multi_page: "%{label} %{model} %{from} - %{to} of %{count} in total"
      multi_page_html: "%{label} %{model} <b>%{from}&nbsp;-&nbsp;%{to}</b> of <b>%{count}</b> in total"

    # models:
    #   entry:
    #     zero:  entries
    #     one:   entry
    #     few:   entries
    #     other: entries

    # line_item:
    #   page_entries_info:
    #     single_page:
    #       zero:  "Your shopping cart is empty"
    #       one:   "Displaying one item in your cart"
    #       other: "Displaying all %{count} items"
    #     multi_page: "Displaying items %{from} - %{to} of %{count} in total"
